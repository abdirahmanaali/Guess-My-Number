// ==> WHAT IS DOM AND DOM MANIPULATION

// DOM => Is stands for Document Object Model and it structured representation of HTML documents. It allows JS to access HTML elements and CSS styles to manipulate them

// DOM tree corresponds to HTML tree.
// Document serves us the entery point

// ==> SELECTING AND MANIPULATING ELEMENTS

// ==> HANDLING CLICK EVENTS

// An EVENT is something that happens on the page. for ex a mouse click or mouse moving or key press or many other events
// An EVENT LISTENER - waits for a certain event to happen and then react to it.
// ADD EVENT LISTENER - is one of the ways to listen for events in JS. It takes to argument one is what it is listening and what it should with it
